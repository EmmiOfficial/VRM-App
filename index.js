import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import VTuberController from './VTuberController';

ReactDOM.render(
  <React.StrictMode>
    <VTuberController />
  </React.StrictMode>,
  document.getElementById('root')
);